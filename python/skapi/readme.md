# songkick api responses

#parse user concerts and plans seem identical
#could be reused
#try to use only two models with same capitalization everywhere

{
    result: [],
    page,
    totalCount,
    pageCount,
    nextPage
}